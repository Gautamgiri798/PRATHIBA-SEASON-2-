# Copy this file to .env.local for local development.
# On your deployment platform (e.g. Railway, Render, Fly.io), set this under Environment Variables.

# Passcode required to access the results dashboard at `/results`.
# If not specified, it will default to 'admin123' (change this in production!).
ADMIN_PASSCODE=your-secret-admin-passcode-here
